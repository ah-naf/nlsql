name: CI/CD Pipeline

on:
  push:
    branches: "**"
  pull_request:
    branches: "**"

jobs:
  hello_world:
    runs-on: ubuntu-latest

    steps:
      - name: Hello World
        run: echo "Hello World!"
